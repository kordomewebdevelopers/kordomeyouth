// export const generateTOTP = () => {
//   return new OTPAuth.Secret({ size: 15 });
// };

// export const verifyTOTP = (secret = "", token = "") => {
//   return new OTPAuth.TOTP({ secret }).validate({ token });
// };
